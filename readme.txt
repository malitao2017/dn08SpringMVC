座springMVC的相关知识：

http://127.0.0.1:8080/dn08SpringMVC

SpringMVC 的原理： 
整体控制：  DispatcherServlet
流程：
request -> HandleMapping(配置) -> Control -> ModelAndView(返回对象) -> ViewResolver(配置) -> View ->response

